fn main(){
    let mut array = [1,2,3,4,5];
    bubbleSort(&mut array);
    
    println!("bubble sort of array is {:?}", array);
}

fn bubbleSort( arr: &mut [i32]) -> &mut [i32]{
    //take first val
    let num0 = arr[0];

    //loop to compare with other vals
    for i in arr{
        if arr[i] > num0;

    }
    //update the array

}